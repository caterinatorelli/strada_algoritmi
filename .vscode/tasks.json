{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "builder",
            "command": "/usr/bin/gcc",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "-std=c90",
                "-Wall",
                "-Wpedantic",
                "${file}",
                "-o",
                "${workspaceFolder}/bin/app.exe"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Attività generata dal debugger."
        }
    ],
    "version": "2.0.0"
}